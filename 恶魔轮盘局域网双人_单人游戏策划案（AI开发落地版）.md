# 恶魔轮盘局域网双人/单人游戏策划案（AI开发落地版）

## 一、项目概述

### 1\.1 游戏定位

一款轻量化、高紧张度的**恶魔轮盘博弈类网页小游戏**，基于局域网实现多人交互，同时兼容单人人机模式。核心玩法依托经典俄罗斯轮盘博弈机制，加入道具、容错、博弈心理对抗元素，整体流程简短、对局节奏快，适配浏览器即开即玩，无需安装客户端。

本项目为**纯网页端局域网游戏**，支持两种游玩模式：单人VS人机AI、双人局域网联机对战，所有功能基于前端\+轻量服务端实现，适配本地局域网设备互通，无外网依赖。

### 1\.2 核心目标（AI开发专用）

- 实现**浏览器端跨设备局域网联机**，同一WiFi/局域网下两台设备可精准同步游戏状态

- 完整实现单人人机博弈、双人玩家博弈双模式，模式可一键切换

- 保证游戏帧同步、状态同步稳定，无卡顿、无数据错乱、无对局掉线重连bug

- 轻量化开发，代码结构清晰、模块化，适配AI直接迭代开发

- UI简洁适配PC/移动端浏览器，兼容主流Chrome、Edge、手机自带浏览器

### 1\.3 运行环境

- 运行平台：所有支持HTML5的网页浏览器（PC/手机）

- 网络环境：同一局域网（同一路由器WiFi/有线局域网），无需外网

- 设备适配：双设备自由配对（PC\+PC、PC\+手机、手机\+手机）

## 二、游戏核心玩法与规则

### 2\.1 基础设定

核心载体：六孔左轮轮盘，每局随机装填**4~6发子弹**，实弹与空弹数量相差不超过1发（4发=2实2空，5发=2实3空或3实2空，6发=3实3空），子弹位置随机生成，保证对局随机性。

对局双方：玩家A / 玩家B（双人模式）、玩家 / 人机AI（单人模式）

胜利条件：一方触发实弹扣血淘汰，另一方直接获胜；血量归零则对局结束。

基础血量：双方初始固定2点血量，血量为0直接判负。

### 2\.2 对局流程（通用）

1. **开局初始化**：系统随机生成本轮子弹数量与位置，重置双方血量、道具，随机决定先手回合

2. **装填展示**：开局及每次重新装填时，向双方展示本局实弹/空弹数量，播放装填动画后方可行动

3. **回合制博弈**：双方轮流操作，每回合玩家可选择「对自己开枪」「对对手开枪」「使用道具」

4. **开枪判定逻辑**：命中实弹扣1点血量；对己开枪为空弹则**继续自己的回合**，对己开枪为实弹则自己扣血并轮到对手；对对手开枪（空弹或实弹）均轮到对手回合，实弹时对手扣血

5. **装填循环**：本轮子弹全部打完后自动重新装填新一轮弹药（再次展示数量），回合保持不变，进入下一循环

6. **对局结束**：任意一方血量归零，立刻终止对局，展示胜负结果，支持一键重新开局

### 2\.3 游戏道具系统（基础道具，可扩展）

每局开局双方获得相同配置的道具（默认每种1个），道具可叠加持有，每回合仅可使用1个道具。
点击"单人模式"或"创建房间"时会弹出对战配置：可分别设置看破/护盾/退蛋/双倍威力的数量（各0~3个）与初始血量（1~5），配置自动保存（联机以房主配置为准）。

- **看破**：查看当前轮盘下一发子弹是空弹/实弹

- **双倍威力**：自己下一发实弹伤害翻倍，仅在自己的回合内生效（回合结束未命中则作废），可击穿护盾（护盾失效并受1点伤害）

- **护盾**：抵挡下一次实弹伤害，单次对局仅生效1次

- **退蛋**：主动退出发射位的一发子弹（可看到实/空弹），不造成伤害，使用后继续自己的回合

### 2\.4 单人模式规则

玩家VS内置AI，AI具备基础博弈逻辑：低血量保守使用道具、高血量主动开枪、预判子弹概率操作，难度为基础标准，无变态操作，保证游玩平衡性。单人模式无需联网，本地完整运行所有游戏逻辑。

### 2\.5 双人局域网联机规则

- 分为**房主**和**访客**：房主创建房间，生成局域网房间码/局域网IP端口，访客输入信息加入房间

- 房间最大人数2人，满员自动开局，空房间支持等待加入

- 所有游戏数据（子弹位置、血量、道具、回合状态）由服务端统一同步，双端完全一致

- 支持中途退出，一方退出则对局自动判定另一方胜利

## 三、整体技术架构（AI开发核心）

### 3\.1 技术栈选型（固定，AI严格遵循）

#### 前端技术栈（客户端）

- 基础架构：HTML5 \+ CSS3 \+ JavaScript \(ES6\+\)

- UI样式：原生CSS/ Tailwind CSS（轻量化，无需框架）

- 动画实现：原生JS \+ CSS动画（轮盘转动、开枪、胜负特效）

- 网络通信：WebSocket（核心局域网同步）

- 本地存储：LocalStorage（记录对局记录、偏好设置）

#### 服务端技术栈（局域网服务）

- 运行环境：Node\.js（轻量、易部署、适配局域网）

- 通信框架：ws 模块（原生WebSocket，无冗余依赖）

- 服务能力：局域网IP监听、房间管理、状态同步、数据校验

#### 核心运行逻辑

服务端部署在局域网任意设备（电脑/本地服务器），所有浏览器客户端通过局域网IP\+端口连接服务端，由服务端统一管控游戏状态，实现多端同步；单人模式可脱离服务端，纯前端本地运行。

### 3\.2 项目架构分层（AI开发目录规范）

整体分为三层，完全模块化，低耦合，方便迭代维护

1. **表现层（前端页面）**：负责UI渲染、动画展示、用户点击交互、页面跳转

2. **逻辑层（前端游戏核心）**：本地游戏规则、AI逻辑、道具逻辑、输入校验

3. **通信层（服务端\+WebSocket）**：房间管理、状态同步、数据校验、联机交互

## 四、核心技术要点（AI开发重点，必须实现）

### 4\.1 局域网联机核心技术

#### 1\. 局域网IP自动监听与连接

- 服务端自动获取本机局域网内网IP（如192\.168\.1\.x），固定端口（默认8080）

- 房主启动服务端后，页面自动展示当前局域网连接地址与房间码

- 同一局域网内设备通过「IP\+端口」或房间码即可精准连接，无需外网穿透

#### 2\. 房间系统机制

- 支持单房间2人匹配，服务端统一生成唯一房间ID

- 状态管控：等待中、对局中、对局结束三种状态，禁止重复开局、禁止离线操作

- 玩家身份绑定：自动区分房主/访客，同步双方设备状态

#### 3\. 游戏状态帧同步机制（核心重点）

采用**服务端权威同步**，所有核心游戏数据由服务端计算下发，客户端仅做渲染，杜绝数据作弊、错乱。

- 同步数据项：轮盘子弹数组、双方血量、双方道具列表、当前回合玩家、对局状态

- 同步时机：每一次操作（开枪、用道具、回合切换）完成后，服务端全量广播最新状态

- 客户端逻辑：接收服务端数据后，覆盖本地所有状态，再执行动画渲染

#### 4\. 断线重连机制

- 客户端断线自动重连（3秒轮询重连）

- 短时间断线（5秒内）重连后自动恢复对局状态，不重置进度

- 长时间断线（超过5秒）判定为主动退出，对局结束

### 4\.2 单人AI核心技术要点

纯前端本地AI，无需服务端支持，独立运行完整对局逻辑

- **概率判定逻辑**：AI根据当前空弹/实弹比例，计算开枪风险，动态选择操作

- **血量策略逻辑**：AI血量1点时优先使用护盾、看破道具，保守操作；满血时主动进攻

- **道具使用逻辑**：优先使用看破获取子弹信息，再决策开枪方式，杜绝无效道具消耗

- **回合合规校验**：AI严格遵循回合规则，无越权操作、无作弊逻辑

### 4\.3 核心游戏逻辑技术实现

#### 1\. 轮盘子弹随机算法

- 每轮随机生成4~6发子弹，实弹与空弹数量相差不超过1发，位置随机打乱

- 每次开枪后，轮盘指针自增，按顺序判定下一发子弹，不重新随机

- 全部子弹打完自动重新装填（新一轮数量重新随机），进入下一循环

- 支持换位道具打乱当前剩余子弹顺序，重新生成随机数组

#### 2\. 操作合法性校验

所有操作（开枪、用道具）均做双层校验：前端即时拦截\+服务端二次校验，禁止超时操作、非回合操作、无道具使用道具等违规行为。

#### 3\. 动画与逻辑解耦

游戏逻辑判定优先于动画，数据结算完成后，再执行轮盘转动、开枪、扣血动画，保证**数据精准，动画流畅**，避免动画卡顿导致逻辑错位。

### 4\.4 数据安全与一致性要点

- 双人模式下，所有核心结算逻辑（子弹判定、扣血、道具生效）均在服务端执行，客户端仅渲染

- 单人模式逻辑封闭在前端，无外部数据交互，保证本地运行稳定

- 每次状态同步携带时间戳，避免旧数据覆盖新数据

- 禁止客户端自主修改游戏核心数据，杜绝本地作弊

## 五、页面功能模块拆分（AI分模块开发）

### 5\.1 首页模块

- 模式选择：单人模式、双人联机模式

- 联机入口：创建房间、加入房间（输入IP/房间码）

- 游戏规则弹窗、重置对局、音效开关

### 5\.2 房间模块

- 房主：展示房间ID、局域网IP、等待玩家提示、开局按钮

- 访客：输入房间信息、加入等待、退出房间按钮

- 双端同步玩家状态（在线/离线、准备状态）

### 5\.3 游戏对局模块

- 轮盘可视化展示、子弹状态动态渲染

- 双方血量展示、道具栏展示

- 回合提示、操作按钮（对己开枪、对敌开枪、使用道具）

- 对局日志（记录每一步操作结果）

### 5\.4 结算模块

- 胜负弹窗展示、对局统计

- 一键重开、返回首页按钮

## 六、开发优先级与落地顺序（AI执行顺序）

1. **基础框架搭建**：前端页面结构、Node\.js服务端基础、WebSocket通信调试

2. **单人模式开发**：轮盘逻辑、子弹算法、道具逻辑、AI博弈、对局结算

3. **局域网联机开发**：房间系统、状态同步、断线重连、双端数据统一

4. **UI动画优化**：轮盘动画、开枪特效、状态提示、适配多端

5. **bug修复与校验**：操作校验、数据同步校验、异常场景兼容

6. **功能迭代扩展**：新增道具、难度调节、对局记录等拓展功能

## 七、异常场景兼容要求（AI必须适配）

- 设备网络波动、断线重连不崩溃、不重置对局

- 双人同时操作、重复点击无逻辑bug

- 房间人数异常、非法加入房间自动拦截

- 手机/PC端适配，页面无错位、按钮无遮挡

- 浏览器刷新后，联机模式可重连恢复对局，单人模式重置对局

## 八、可拓展预留功能（后续迭代）

- 自定义血量、自定义子弹数量，调节对局难度

- 更多博弈道具、恶魔诅咒buff机制

- 对局数据统计、胜率记录、本地存档

- 多房间匹配、多人观战模式

> （注：部分内容可能由 AI 生成）
